# /judge-tests

Input: plan, current diff, test changes, and command output.

Invoke the Test Judge Agent. Map every acceptance line to proof. Return `APPROVED`, `CHANGES_REQUESTED`, or `INSUFFICIENT_EVIDENCE` with exact commands and missing cases.

Do not fix code or tests during this command.
