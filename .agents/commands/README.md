# Commands

These command contracts provide stable entry points into the delivery workflow:

- `/ground-change`
- `/draft-plan`
- `/judge-tests`
- `/review-change`
- `/prepare-pr`

Each command returns an artifact or verdict. No command silently advances the next gate.
