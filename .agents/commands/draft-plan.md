# /draft-plan

Input: completed grounding record and requested outcome.

Invoke `$draft-implementation-plan`. Write the next numbered plan under `docs/plans`. Return its path, mapped requirements, open decisions, and whether the plan was committed and pushed.

Do not implement the change through this command.
