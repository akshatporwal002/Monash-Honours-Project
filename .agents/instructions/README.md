# Instructions

`core.md` is the common operating contract for every agent. Role-specific duties live in `../agents`, while strict access and safety limits live in `../permissions` and `../guardrails`.
