# Browser requests use Vite's /api development proxy by default.
VITE_API_BASE_URL=/api/v1
# Same-origin mutations reuse the readable ql_csrf cookie issued by the API.
# Never put API keys, pseudonyms, direct identity, answers, or drafts in VITE_* variables.
