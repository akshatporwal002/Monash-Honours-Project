# Learner start, structured tasks and reviewed support

## Delivered behavior

New assessed work opens as a preview of the published conditions. The learner must activate **Start assessed task** before entering a response, requesting instructional support, saving or submitting. Existing started drafts revalidate their original displayed form through the idempotent start endpoint. Retry and version-conflict behavior preserves the original work reference and local edits.

`matching` and `sequencing` are first-class task types. Definitions contain stable item IDs, text and source-passage references. Matching is one-to-one; sequencing contains each declared item once. Versioned JSON responses retain item IDs and optional exact reviewed labels in the existing protected answer field. Partial drafts are allowed; submissions require complete, unique, known items. Extra fields, invented labels, invalid keys and undeclared item sources are rejected.

Educators can select either type for generation and edit its definition and private answer key in task review. Learners use native selects or move buttons; saved evidence uses a table or ordered list with the submitted labels. The independent handler compares exact pairs/order when a private key is supplied. Formal responses continue through the approved criterion and human-review path; a type-handler match never confirms a result.

The offline generator creates bounded source-excerpt matching and reconstruction exercises. These are formative drafts, not evidence of pedagogical validity. Generation version `task-generation-v2` requires purpose, difficulty basis, intended evidence, expected response features, tools, instructional support, access modes, equivalent formats and rubric version. Task, source, provider/model and reviewed-rubric versions remain in existing immutable revision and source-use records. Removing the teaching design blocks approval of a v2-generated task. Older reviewed generator versions retain their existing contract.

Reviewed episode plans can contain text, visual, worked-example, circuit and stepwise instructional representations. Each has source references, an equivalence rationale, equivalent text and an explicit support level. Diagram steps and circuit operations have text equivalents. The server releases only the requested frozen item. The existing support receipt stores its original form and item index; evidence capture resolves the support level from that frozen plan. Repeated use is allowed without a result penalty. Fresh transfer hides both new requests and replayed instructional content, while existing approved access support remains available.

Tutor dialogue creates a normal-priority review case for repeated answer-only requests or explicit copied-solution language. The cue preserves the relevant turn IDs, rule version and uncertainty. Cases are deduplicated by learner, task, context and signal kind. The response redirects to reasoning. Neither the cue nor its review case changes assessment evidence or results, and neither asserts misconduct. This is a limited dialogue-language detector, not plagiarism or authorship detection.

## Scope that remains open

| Area | Supported boundary and remaining gap |
| --- | --- |
| FR8 generation | The selector offers the six existing basic response types plus matching/sequencing. The local generator rejects episode-only prediction/reasoning/explanation/revision/reflection/transfer generation with an authoring action instead of silently producing a keyword task. Their existing typed manual episode flows remain. Complete automatic generation of every assessed multipart definition, approved Bloom target and criterion anchor remains open. |
| FR9/PD4 types | Matching/sequencing now have authoring, generation, draft/submission, rendering and evaluation contracts. `state_comparison`, `diagnosis`, `probability_interpretation`, `part_complete` and standalone `confidence` remain staged identifiers; related evidence can still be collected through existing circuit/code/episode activities. |
| FR35 equivalence | The new forms are reviewed instructional support for the supported episode stage. They do not automatically establish equivalent accessible forms for every construct or transfer task. Actual content/source approval and construct-equivalence review remain required. |
| FR22/FR39 learning/calibration | Validated changes in understanding/reasoning and confidence calibration are not established by weekly counts or confidence records. This delivery adds no validated learning-gain or calibration estimator. |
| PD3 strategy/useful format | Strategy success/failure and which format improves a learner's understanding remain unproven. Opening support, choosing a format, repeating or revising is observed activity, not evidence that the strategy worked. |
| Human evidence | Named assistive-technology checks, expert content/criterion validity, and institutional/source/form activation approvals remain external records. Synthetic tests establish software behavior only. |

## Contracts and migration

- New schemas: `structured_tasks.py`, `generated_task_design.py`, `support_representations.py`.
- Public additions: `TaskType` values, `TaskRead.structured_task`, `EpisodeStateRead.representation_choices`, `EpisodeHelpUseReceipt.representation` and reviewed `EpisodePlanV1.support_representations`.
- Migration `20260911_0050` expands the task CHECK constraint, follows `20260911_0049`, and is included in head `20260911_0051`. It preserves rows, indexes, triggers and foreign keys and is replay-safe. Downgrade requires restoring a verified backup.
- API/frontend contracts are regenerated during integration. Provider accounting, moderation and study/export behavior are documented in their respective deliveries.

## Review follow-ups

New-work App and browser scenarios now explicitly start assessed work; resumed draft scenarios retain their existing flow. Generated matching/sequencing definitions use unrelated UUIDv4 identifiers and independently shuffled display groups, while saved keys and revisions retain their relationships. Inherited support evidence resolves the maximum applicable declaration from the exact frozen approved receipts, with the existing time/stage filters and provenance links. Worked-example level 4 and stepwise level 3 carry through checkpoints, simulations, responses and supported process evidence; unaided transfer remains level 0. Historical immutable evidence is not rewritten.

Focused follow-up checks: the formal-start App selector passed; eight typed-ID/support cases passed; eighteen choice-authoring/API/handler cases passed; two extended legacy-history/failed-edit API cases passed; two formal-choice API cases and one existing start/freeze case passed; two missing-choice UI cases and the existing multiple-answer submission selector passed. These are overlapping targeted runs, not a complete frontend, backend or browser suite. TypeScript, scoped ESLint and Ruff checks passed. The shared synthetic assessment-definition fixture now correctly uses `short_answer` for its prose-analysis task instead of a choice-less `quiz`.

## Remaining software checklist

This inventory began at delivery `984a9ec`; the choice-validation row records the later bounded follow-up. It separates code that is absent from deliberately limited behavior; it is not additional executed test evidence. Integration with other deliveries may close individual entries.

| Priority / requirement | Remaining software action | Current boundary and completion condition |
| --- | --- | --- |
| Closed / FR9 | Canonical declared choices and unavailable-task state. | New authoring/review requires at least two choices with unique nonblank string IDs and nonblank text. Drafts and practice/formal submissions validate exact membership; MCQ uses one exact ID, multiple-answer uses a JSON array of distinct string IDs. Blank drafts are allowed and empty submissions rejected. Present answer keys must be valid and mutually consistent. The UI no longer invents demo choices and blocks writes when choices are unavailable. Existing immutable legacy answers remain readable in their original shape; malformed legacy definitions require an educator repair/review before new work. No historical records are rewritten or Boolean matches promoted to formal results. |
| Partial / FR8 | Extend typed multipart generation beyond the first family. | The [source-led generation delivery](multipart-generation-candidates.md) adds required basic text activities, general source-application multipart candidates, exact-revision variants and real-feedback-conditioned drafts. Basic transfer has a usable typed practice control; assessed multipart transfer retains real work references. Semantic alignment remains subject to source and task quality review. |
| Partial / FR8 | Extend generated assessed-design authoring beyond the first bridge. | The [multipart candidate delivery](multipart-generation-candidates.md) proposes SUMMATIVE metadata, Apply/procedural target, five criteria/anchors, ALL_OF rule, conditions and review guidance. The bridge creates an unapproved definition in the existing store with exact form/source provenance; formal eligibility and access/Bloom verification remain unset. The general source-application family and linked lossless definition editor extend this bridge. Local templates propose Apply/procedural; target and criterion approval remain assessor decisions. |
| 2 / FR8 | Replace generic generation placeholders with type-specific candidate content and evidence guidance. | Source-led activity demands and response guidance now exist; code/circuit templates choose supported H/X/CX gates named in the source instead of fixed Hadamard content. Equivalent representation generation belongs to the separate support delivery. Matching/sequencing currently use bounded source-excerpt recognition/reconstruction. Source-ID and schema validation do not check semantic alignment. Add source-specific prompts, response features and answer/rubric consistency checks per type; retain educator review. The v2 metadata presence gate is implemented, while older approved generator versions retain their previous contract. |
| 3 / FR35, FR8 | Deliver reviewed equivalent representations outside the assessed supported-stage hint path. | New representations require an `EpisodePlanV1` and an assessment work reference. There is no equivalent representation delivery path for ordinary formative/practice tasks, no generated representation-content contract in the local generator, and no new typed per-stage access-representation contract for unaided transfer. Add scoped, versioned delivery for these cases with access support separated from instructional content. A list of format names is not delivered equivalent content. |
| 3 / FR35 | Apply actual presentation preferences to representation delivery. | The learner can explicitly request any offered representation. `format` and `explanation_detail` do not select or vary its actual content; existing preference effects concern generic workspace guidance. `support_amount=on_request` wraps legacy hint controls but not the new representation controls. Bind preferences to available reviewed variants/disclosure, explain unavailable choices, retain override, and preserve all required evidence. Existing break/repeat/pace controls and feedback-form presentation should be retained. |
| 4 / PD4 | Either retain explicit staging or implement the five remaining standalone forms. | `state_comparison`, `diagnosis`, `probability_interpretation`, `part_complete` and `confidence` are staged identifiers, not registered task types. They lack standalone enum/schema, authoring/generation, response control and evaluator/lifecycle contracts. Related circuit, code, prediction and confidence evidence is not proof of these standalone forms. PD4 permits staging; select the next form deliberately rather than adding names without their full path. Matching/sequencing are no longer on this missing list. |
| 4 / PD6 | Extend cue coverage if the requirement is intended beyond recent tutor dialogue. | Repeated-request detection checks the current message and only the two most recent turns in the same context. Copied-solution detection recognizes a small set of explicit copying/AI-boilerplate phrases in tutor messages. There is no spaced-history detector or copied-solution signal producer on submitted answer/code evidence. Add bounded, explainable producers and source links if needed; retain deduplicated review cases, alternative explanations and zero automatic penalty. No plagiarism/authorship verdict is required or justified. |

No additional missing top-level FR9 task name was identified beyond the delivered matching/sequencing extension: the named basic and episode families already exist. This does not establish complete strict validation or accessible lifecycle/evaluator coverage for every retained family. Matching/sequencing currently support an independent exact-key handler and the existing human formal-criterion path; wiring a Boolean match directly to a formal result is not a required follow-up.

The explicit new-work start correction has no identified remaining software gap in this delivery. Coordinator-owned migration ordering, readiness pinning, contract regeneration and scanner-fixture integration remain integration work rather than learner feature omissions.

## Approval and validation dependencies

- **FR8/FR9:** actual authorised sources, task forms, Bloom targets, criterion anchors and assessor decisions are human records. Software can generate and validate draft candidates before those records exist; fixture approval must never stand in for activation.
- **FR35:** an authorised reviewer must establish construct equivalence, permitted instructional/access support and transfer conditions for actual content. Named screen-reader/native-keyboard evidence remains manual validation. These dependencies do not remove the missing delivery/preference work above.
- **PD6:** existing queue machinery can retain cues without an automatic finding. Actual queue owners and any institutional response process need real assignments. Broader cue usefulness and false-positive interpretation require evaluation; no external plagiarism service or institutional misconduct approval is implied by this implementation.
- **Learning/calibration/strategy:** validated learning-change and confidence-calibration measures, strategy success/failure and useful-format inference remain unestablished. Implementing a metric or linking more records would be software work; claiming that it measures learning or effectiveness requires separate validation. Activity counts and support preferences establish neither.

## Focused verification

`test_structured_tasks.py` covers exact evaluation, invalid payloads, grounded generation, review requirements, partial drafts, reloads, revisions and formal frozen-version conflicts. `test_structured_tasks_migration.py` verifies a populated forward upgrade, preserved protected history/triggers, both allowed values, unknown-type rejection, replay and downgrade refusal.

`test_task14_support.py` covers all five representations, exact frozen support levels, idempotent history and transfer replay suppression. `test_tutor.py` covers uncertain review cues, deduplication and unchanged assessment work.

Frontend checks cover explicit keyboard start and retry behavior, existing saved typed episode results, partial matching drafts and readable saved labels, keyboard sequencing, representation request/transfer hiding, and the generation selector. Type checking and scoped lint were run. No full backend, frontend or browser suite, paid-provider call, deployment or real study processing was performed for this delivery.
